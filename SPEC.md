# みっちー財政カルテ 仕様書

**最終更新：2026年9月7日**
※ 公開中の実ファイルを読み取って作成

---

## ⚠️ 最重要：公開には手動の一手が必要

Pages の公開元を GitHub Actions に切り替えたため、**ファイルを更新しただけでは公開されません。**

**GitHubの仕様上、ワークフロー（bot）がコミットした変更は、別のワークフローを起動しません。** 無限ループを防ぐための安全装置です。

| 操作 | 自動で公開されるか |
|---|---|
| Rayさんが手でファイルを編集・アップロード | ✅ 自動で公開される |
| `preview` / `apply` を実行 | ❌ **手動実行が必要** |
| `update-data.yml`（年次更新）を実行 | ❌ **手動実行が必要** |

**手動実行の手順**
1. **行動（Actions）** → **「構文チェック後にデプロイ」**
2. **ワークフローを実行する** → 実行
3. 1〜2分で公開

これを忘れると、データは更新されているのにサイトに反映されません。

---

## 基本情報

| 項目 | 内容 |
|---|---|
| 公開URL | https://ray-ray870.github.io/mitchie-fiscal/ |
| リポジトリ | https://github.com/ray-ray870/mitchie-fiscal |
| ホスティング | GitHub Pages（**Source は「GitHub Actions」**） |
| GA4 測定ID | G-5BGX6KQZFL |
| キャッシュ版数 | `?v=202607251024` |

---

## ファイル構成

| ファイル | 行数 | 役割 |
|---|---|---|
| `index.html` | 129行 | 骨組み・メタタグ |
| `style.css` | 141行 | 見た目（**デザイン変更はここだけ**） |
| `app.js` | 1,627行 | 診断ロジック・描画 |
| `images.js` | — | みっちー画像（Base64） |
| `manifest.json` | — | PWA設定 |
| `data-*.json` | 6ファイル | 財政データ |
| `SPEC.md` | — | 本書 |

### 外部読み込み（順序厳守）

1. GA4 タグ
2. `images.js`
3. html2canvas 1.4.1（CDN）
4. `app.js?v=...`

**`app.js` は必ず `images.js` と html2canvas より後。** 順序が入れ替わると診断画像の生成が失敗する。deploy-with-check がこの順序を検査している。

---

## データ構成

**全1,786エントリ**（全国の市区町村＋都道府県。東京23区も個別収録）

| ファイル | 件数 |
|---|---|
| `data-hokkaido-tohoku.json` | 413 |
| `data-chubu.json` | 325 |
| `data-kanto.json` | 323 |
| `data-kyushu.json` | 281 |
| `data-kinki.json` | 234 |
| `data-chugoku-shikoku.json` | 210 |

- 財政データ：**令和6年度**まで
- 人口データ：**令和7年**まで
- 出典：総務省「地方財政状況調査関係資料」

### JSONキー構造

| キー | 内容 |
|---|---|
| `p` | 都道府県名（**都道府県は `p` が自分自身の名前と一致する**） |
| `f` | 財政力指数 |
| `d` | 実質公債費比率（%） |
| `x` | 経常収支比率（%） |
| `u` | 将来負担比率（%） |
| `r` | 財政調整基金残高（億円） |
| `g` | 人口増減率（%） |
| `pop` | 人口 |
| `eo` / `ei` | 歳出 / 歳入（億円） |
| `eog` / `eig` | 歳出増減率 / 歳入増減率（%） |
| `edu` | 教育費一般財源比率（%） |
| `ch` | 子ども1人当たり投資額（万円） |

末尾に `_r1`〜`_r6` を付けたキーが過去年度の値。最大8データポイントのローリング更新。

**都道府県の判定方法：** 読み込み時に `DB[k].p === k` を調べ、一致するものに `__pref = true` を立てている。

### 特殊処理

- **泊村**：北海道・青森県の名称衝突に対応
- **梼原町 / 須恵町**：異体字ゆらぎに対応
- 廃止済み自治体は除外

### 既知の欠落

**宮崎県 日之影町** と **高知県 大月町** が収録されていない。総務省の公会計データには存在するため、次回の年次更新時に確認すること。

---

## スコア算出

市区町村と都道府県で計算式が異なる。`calcH(f,d,x,u,r,eo,isPref)` の `isPref` が真なら `calcHPref` に分岐する。

### 市区町村（`calcH` 本体）

| 指標 | 配点 | 計算 |
|---|---|---|
| 財政力指数 | 25点 | `f/1.2*25` |
| 実質公債費比率 | 20点 | 25%で0点 |
| 経常収支比率 | 20点 | 100%で0点、85%で満点 |
| 将来負担比率 | 20点 | 200%で0点、0以下で満点 |
| 基金比率 | 15点 | 歳出比15%で満点 |

結果：1,739件の中央値66点、27〜100点に分布。

### 都道府県（`calcHPref`）

市区町村基準では47県中46県が下位2段階に固まっていたため新設。都道府県は高校・国道・河川など大規模資産を抱え、将来負担比率や経常収支比率の水準が構造的に高い。

| 指標 | 配点 | 0点ライン | 満点ライン | 47県の中央値 |
|---|---|---|---|---|
| 財政力指数 | 25点 | 0.20 | 0.90 | 0.47 |
| 実質公債費比率 | 20点 | 20% | 6% | 11.0% |
| 経常収支比率 | 20点 | 101% | 86% | 93.8% |
| 将来負担比率 | 20点 | 340% | 80% | 159.7% |
| 基金比率 | 15点 | — | 歳出比5% | 3.5% |

結果：47県の中央値56点、17〜100点。絶好調1・元気7・しんどい25・ぐったり12・ひんし2。

**市区町村（中央値66点）とは約10点の差が残るが、役割が違う組織なので統一しない。** アプリ内の解説に明記済み。

### 判定ラベル

| スコア | みっちーの状態 |
|---|---|
| 85点以上 | 絶好調 |
| 70点以上 | 元気 |
| 50点以上 | ちょっとしんどい |
| 30点以上 | ぐったり |
| 30点未満 | ひんし状態 |

---

## 指標の色分け

### 財政力指数（`colorF`）

| 範囲 | 色 |
|---|---|
| 0.70以上 | 🟢 緑 |
| 0.45〜0.70 | 🔵 青 |
| 0.25〜0.45 | 🟡 黄 |
| 0.25未満 | 🟠 橙 |

市区町村 緑22%・青28%・黄30%・橙21%／都道府県 緑13%・青45%・黄43%・橙0%。

**根拠：** 中央値は市区町村0.44・都道府県0.47。旧基準（1.0/0.7）では市区町村の78%が橙になり判別できなかった。財政力が低いこと自体は地方交付税で補われる前提の数字であり危険信号ではないため、橙は下位2割程度に絞っている。

### 経常収支比率（`colorX`／市区町村・都道府県 共通）

| 範囲 | 色 |
|---|---|
| 90%未満 | 🟢 緑 |
| 90〜95% | 🔵 青 |
| 95〜98% | 🟡 黄 |
| 98%以上 | 🟠 橙 |

**根拠：** かつて「75〜80%が望ましい」とされたが法令基準ではなく慣例的な目安。社会保障費の増加で全国的に上昇し、2003年度以降は全国平均が90%超。中央値は市区町村91.5%・都道府県93.8%。98%以上は全体の約9%。

### 将来負担比率（`colorU`／基準を分ける）

**市区町村**：負担なし・0以下→緑／60%未満→青／60〜100%→黄／100%以上→橙
**都道府県**：100%未満→緑／100〜160%→青／160〜250%→黄／250%以上→橙

※ 350%以上は法令上の早期健全化基準（都道府県は400%）。

### 変更していないもの

- **実質公債費比率**（10/18/25）… 18%が起債許可、25%が早期健全化基準という**法令上の数値**なので動かさない
- **基金**（30億/10億の金額判定）… 人口規模を無視しているが、比率に変えると横浜市が橙・渡嘉敷村が緑になるなど直感と衝突する。**分母を「標準財政規模」にすれば世間の目安（10〜20%）と直接つながるため、10月の更新でデータ項目を追加してから再検討する方針**

---

## スコアと指標の食い違いを知らせる帯（`noteHtml`）

**スコアバーの直上**に1行表示する。

| 条件 | 文言 |
|---|---|
| 70点以上 かつ 指標が黄または橙 | ⚠️ ただし、〇〇は高い水準です |
| 50点未満 かつ 指標が緑 | 💡 〇〇は健全な水準です |

対象は経常収支比率・将来負担比率・実質公債費比率の3つ。該当は全1,786件中92件（5.2%）。注意と補足が同時に出るケースはゼロ。

例：神奈川県77点「ただし、経常収支比率は高い水準です」／秋田県44点「経常収支比率は健全な水準です」。

---

## 主な機能

- 自治体名オートコンプリート検索
- 都道府県内ランキング・全国ランキング
- 同規模自治体との比較（A〜I の財政類型ラベル）
- データの急増・急落に対する動的な注釈表示
- 財政状況バッジ（起債許可団体・早期健全化団体・財政再生団体）
- 検索履歴（localStorage・5件）
- History API による端末の戻るボタン対応
- 診断結果の画像共有（html2canvas）
- OGP・`?city=` ディープリンク
- PWA（Service Worker なし）

---

## アクセシビリティ

| 対応内容 | 件数 |
|---|---|
| `role="button"` + `tabindex="0"` | 11箇所 |
| 画面非表示の見出し（`.sr-only`） | 3箇所 |
| みっちー画像の説明文（体調名を動的挿入） | 3箇所 |
| 検索候補の読み上げ（`aria-live`） | 1箇所 |
| キーボード操作（Enter / スペース） | 全 `role="button"` 対象 |

---

## 年次データ更新（GitHub Actions 半自動）

**次回リマインダー：10月20日**

1. Claude に新年度のExcel URL（6種類）を探してもらう
2. `scripts/config.json` を編集（URL、`fiscal_year_label`、`population_year_label`）
3. Actions から **`update-data.yml`** を実行（検証成功時のみ自動コミット）
4. **行動 →「構文チェック後にデプロイ」→ ワークフローを実行する** ← **忘れやすい。これをしないと公開されない**
5. `index.html` の年度表記を手動更新

### ⚠️ 毎回必ず確認

**OGPメタタグの自治体数「1,786」は手動更新が必要。**
`og:description` と `twitter:description` の2箇所。共有ボタンの文言はライブデータから自動生成されるが、**OGPのみハードコード**のため反映されない。

詳細手順は `scripts/README.md` を参照。

---

## ワークフロー

| ファイル | 用途 |
|---|---|
| `deploy-with-check.yml` | 公開前チェック＋デプロイ（`push` と手動の両方で起動） |
| `update-data.yml` | 年次データ更新 |

一度きりの作業用ワークフロー（`refactor.yml` `a11y.yml` `colors.yml` `prefscore.yml` `notes.yml` `fcolor.yml`）は完了後に削除済み。

### deploy-with-check の検査項目（7つ）

1. `app.js` の構文
2. `images.js` の構文
3. 必要ファイル11個の存在
4. `style.css` / `app.js` の読み込みタグ
5. 読み込み順序（images.js → html2canvas → app.js）とGA4・OGPタグの残存
6. `<div>` の開閉バランス
7. データ6ファイルの妥当性と**合計1,700件以上**

チェックに落ちた場合は公開されず、前回公開されたサイトがそのまま残る。

---

## 大規模変更のやり方（preview方式）

手作業ではなく、スクリプト＋GitHub Actions で機械的に処理する。

1. `scripts/` にPythonスクリプトを追加
2. `.github/workflows/` にワークフローを追加（`workflow_dispatch`、選択肢は `preview` / `apply`）
3. **preview で実行** → 出力先は `preview/` フォルダ（本番は無傷）
4. **「構文チェック後にデプロイ」を手動実行** ← botのコミットなので自動では走らない
5. `https://ray-ray870.github.io/mitchie-fiscal/preview/` で確認
6. 問題なければ **apply**（`preview/` は自動削除）
7. **再び「構文チェック後にデプロイ」を手動実行**
8. 完了後、使ったワークフローを削除

**スクリプトの必須要件**
- 検証に1つでも失敗したら、ファイルを一切書き込まずに異常終了する
- preview モードでは本番ファイルに触れない
- 二重適用を防ぐガードを入れる（例：`if "colorF" in js: fail(...)`）
- `preview/` から親のデータを読むため、`data-*.json` 等のパスを `../` 付きに書き換える

---

## 開発上の原則

- **フォントサイズと可読性を最優先。** UI変更時に文字が小さくならないよう常に確認する
- 作業環境：Androidスマホのみ（Pixel 9a）。GitHubはブラウザ経由
- 新規ファイルは「Create new file で直接作る」または「Upload files」。既存ファイルの書き換えより、**追加 → 旧ファイル削除**の手順を好む
- GitHubでのファイル移動は「ファイル名の先頭にフォルダ名を足す」
- **削除するファイル名は実行前によく確認する**（deploy-with-check.yml を誤って消した事例あり）

---

## 未着手の課題

| 項目 | 内容 | 優先度 |
|---|---|---|
| インラインstyleのクラス化 | 192箇所のうち143箇所（55種類）が置換可能。残り49箇所は色を動的計算しているため対象外。上位5種類だけで79箇所 | 次にデザインを触るとき |
| 基金の色分け | 金額判定を比率に変えるか、標準財政規模を新規取得して世間の目安（10〜20%）と揃えるか。10月の更新に合わせて検討 | 中 |
| 公会計の追加 | 総務省の市区町村指標一覧（1,741件）・都道府県指標一覧（47件）を取得済み。「みっちーのお家」として折りたたみセクションに、お家の古さ（有形固定資産減価償却率）・ローンの残り（将来世代負担比率）・借金の額・払い終わった割合の4指標を数字のみで表示する方針。イラストは作らない | 検討中 |
| iOSの共有機能 | `navigator.share` / `window.open` / `ClipboardItem` が非同期処理の後に呼ばれており、iOS Safari の「タップ直後」制約に抵触 | 声が届いてから |
| iOS向けCSS | `-webkit-backdrop-filter` 未指定、`dvh` 未使用、`env(safe-area-inset-bottom)` 未指定 | 低 |
| デプロイの完全自動化 | botのコミット後もデプロイが走るようにするには、各ワークフローからデプロイを呼び出す設定が必要。トークン設定を伴うためスマホ作業ではやや面倒 | 低 |
