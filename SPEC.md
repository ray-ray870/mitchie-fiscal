# みっちー財政カルテ 仕様書

**最終更新：2026年9月6日**
※ 公開中の実ファイルを読み取って作成

---

## 基本情報

| 項目 | 内容 |
|---|---|
| 公開URL | https://ray-ray870.github.io/mitchie-fiscal/ |
| リポジトリ | https://github.com/ray-ray870/mitchie-fiscal |
| ホスティング | GitHub Pages（mainブランチ） |
| GA4 測定ID | G-5BGX6KQZFL |
| キャッシュ版数 | `?v=202607251024` |

---

## ファイル構成

| ファイル | 行数 | 役割 |
|---|---|---|
| `index.html` | 129行 | 骨組み・メタタグ |
| `style.css` | 141行 | 見た目（**デザイン変更はここだけ**） |
| `app.js` | 1,616行 | 診断ロジック・描画 |
| `images.js` | — | みっちー画像（Base64） |
| `manifest.json` | — | PWA設定 |
| `data-*.json` | 6ファイル | 財政データ |
| `SPEC.md` | — | 本書 |

### 外部読み込み（順序厳守）

1. GA4 タグ
2. `images.js`
3. html2canvas 1.4.1（CDN）
4. `app.js?v=...`

**`app.js` は必ず `images.js` と html2canvas より後。** 順序が入れ替わると診断画像の生成が失敗する。

---

## データ構成

**全1,786エントリ**（全国の市区町村＋都道府県。東京23区も個別収録）

| ファイル | 件数 |
|---|---|
| `data-hokkaido-tohoku.json` | 413 |
| `data-chubu.json` | 325 |
| `data-kanto.json` | 323 |
| `data-kyushu.json` | 281 |
| `data-kinki.json` | 234 |
| `data-chugoku-shikoku.json` | 210 |

- 財政データ：**令和6年度**まで
- 人口データ：**令和7年**まで
- 出典：総務省「地方財政状況調査関係資料」

### JSONキー構造

| キー | 内容 |
|---|---|
| `p` | 都道府県名（**都道府県は `p` が自分自身の名前と一致する**） |
| `f` | 財政力指数 |
| `d` | 実質公債費比率（%） |
| `x` | 経常収支比率（%） |
| `u` | 将来負担比率（%） |
| `r` | 財政調整基金残高（億円） |
| `g` | 人口増減率（%） |
| `pop` | 人口 |
| `eo` / `ei` | 歳出 / 歳入（億円） |
| `eog` / `eig` | 歳出増減率 / 歳入増減率（%） |
| `edu` | 教育費一般財源比率（%） |
| `ch` | 子ども1人当たり投資額（万円） |

末尾に `_r1`〜`_r6` を付けたキーが過去年度の値。最大8データポイントのローリング更新。

**都道府県の判定方法：** 読み込み時に `DB[k].p === k` を調べ、一致するものに `__pref = true` を立てている。

### 特殊処理

- **泊村**：北海道・青森県の名称衝突に対応
- **梼原町 / 須恵町**：異体字ゆらぎに対応
- 廃止済み自治体は除外

### 既知の欠落

**宮崎県 日之影町** と **高知県 大月町** が収録されていない。総務省の公会計データには存在するため、次回の年次更新時に確認すること。

---

## スコア算出

**市区町村と都道府県で計算式が異なる。** 関数は `calcH(f,d,x,u,r,eo,isPref)`。`isPref` が真なら `calcHPref` に分岐する。

### 市区町村（`calcH` 本体）

| 指標 | 配点 | 計算 |
|---|---|---|
| 財政力指数 | 25点 | `f/1.2*25` |
| 実質公債費比率 | 20点 | 25%で0点 |
| 経常収支比率 | 20点 | 100%で0点、85%で満点 |
| 将来負担比率 | 20点 | 200%で0点、0以下で満点 |
| 基金比率 | 15点 | 歳出比15%で満点 |

結果：1,739件の中央値66点、27〜100点に分布。

### 都道府県（`calcHPref`）

2026年9月6日に新設。**現行の市区町村基準では47県中46県が下位2段階に固まっていたため。** 都道府県は高校・国道・河川など大規模資産を抱え、将来負担比率や経常収支比率の水準が構造的に高い。

| 指標 | 配点 | 0点ライン | 満点ライン | 47県の中央値 |
|---|---|---|---|---|
| 財政力指数 | 25点 | 0.20 | 0.90 | 0.47 |
| 実質公債費比率 | 20点 | 20% | 6% | 11.0% |
| 経常収支比率 | 20点 | 101% | 86% | 93.8% |
| 将来負担比率 | 20点 | 340% | 80% | 159.7% |
| 基金比率 | 15点 | — | 歳出比5% | 3.5% |

結果：47県の中央値56点、17〜100点に分布。絶好調1・元気7・しんどい25・ぐったり12・ひんし2。

**市区町村（中央値66点）とは約10点の差が残るが、役割が違う組織なので統一しない。** アプリ内の解説にその旨を明記している。

### 判定ラベル

| スコア | みっちーの状態 |
|---|---|
| 85点以上 | 絶好調 |
| 70点以上 | 元気 |
| 50点以上 | ちょっとしんどい |
| 30点以上 | ぐったり |
| 30点未満 | ひんし状態 |

---

## 指標の色分け

2026年9月6日に見直し。関数は `colorX`（経常収支比率）と `colorU`（将来負担比率）。

### 経常収支比率（市区町村・都道府県 共通）

| 範囲 | 色 |
|---|---|
| 90%未満 | 🟢 緑 |
| 90〜95% | 🔵 青 |
| 95〜98% | 🟡 黄 |
| 98%以上 | 🟠 橙 |

**根拠：** かつて「75〜80%が望ましい」とされてきたが、これは法令上の基準ではなく慣例的な目安。社会保障費の増加により全国的に上昇し、2003年度以降は全国平均が90%を超え続けている。現在の中央値は市区町村91.5%、都道府県93.8%。98%以上は全体の約9%。

### 将来負担比率（基準を分ける）

**市区町村**

| 範囲 | 色 |
|---|---|
| 負担なし・0以下 | 🟢 緑 |
| 60%未満 | 🔵 青 |
| 60〜100% | 🟡 黄 |
| 100%以上 | 🟠 橙 |

**都道府県**

| 範囲 | 色 |
|---|---|
| 100%未満 | 🟢 緑 |
| 100〜160% | 🔵 青 |
| 160〜250% | 🟡 黄 |
| 250%以上 | 🟠 橙 |

※ 350%以上は法令上の早期健全化基準（都道府県は400%）。

### 変更していない色分け

- **実質公債費比率**（10/18/25）… 18%が起債許可、25%が早期健全化基準という**法令上の数値**なので動かさない
- **財政力指数**（1.0/0.7）… 市区町村の78%が橙になる問題があるが未着手
- **基金**（30億/10億）… 金額で判定しているため人口規模を無視している。未着手

---

## スコアと指標の食い違いを知らせる帯

2026年9月6日に追加。関数は `noteHtml(d, h, isPref)`。**スコアバーの直上**に1行表示する。

| 条件 | 文言 |
|---|---|
| 70点以上 かつ 指標が黄または橙 | ⚠️ ただし、〇〇は高い水準です |
| 50点未満 かつ 指標が緑 | 💡 〇〇は健全な水準です |

対象指標は経常収支比率・将来負担比率・実質公債費比率の3つ。

**該当は全1,786件中92件（5.2%）。** 注意と補足が同時に出るケースはゼロ。

例：神奈川県77点「ただし、経常収支比率は高い水準です」／秋田県44点「経常収支比率は健全な水準です」。

---

## 主な機能

- 自治体名オートコンプリート検索
- 都道府県内ランキング・全国ランキング
- 同規模自治体との比較（A〜I の財政類型ラベル）
- データの急増・急落に対する動的な注釈表示
- 財政状況バッジ（起債許可団体・早期健全化団体・財政再生団体）
- 検索履歴（localStorage・5件）
- History API による端末の戻るボタン対応
- 診断結果の画像共有（html2canvas）
- OGP・`?city=` ディープリンク
- PWA（Service Worker なし）

---

## アクセシビリティ

2026年8月21日実施。見た目は変更していない。

| 対応内容 | 件数 |
|---|---|
| `role="button"` + `tabindex="0"` | 11箇所 |
| 画面非表示の見出し（`.sr-only`） | 3箇所 |
| みっちー画像の説明文（体調名を動的挿入） | 3箇所 |
| 検索候補の読み上げ（`aria-live`） | 1箇所 |
| キーボード操作（Enter / スペース） | 全 `role="button"` 対象 |

---

## 年次データ更新（GitHub Actions 半自動）

**次回リマインダー：10月20日**

1. Claude に新年度のExcel URL（6種類）を探してもらう
2. `scripts/config.json` を編集（URL、`fiscal_year_label`、`population_year_label`）
3. Actions から `update-data.yml` を実行（検証成功時のみ自動コミット）
4. `index.html` の年度表記を手動更新

### ⚠️ 毎回必ず確認

**OGPメタタグの自治体数「1,786」は手動更新が必要。**
`og:description` と `twitter:description` の2箇所。共有ボタンの文言はライブデータから自動生成されるが、**OGPのみハードコード**のため反映されない。

詳細手順は `scripts/README.md` を参照。

---

## ワークフロー

| ファイル | 用途 |
|---|---|
| `.github/workflows/update-data.yml` | 年次データ更新 |

※ 一度きりの作業用ワークフロー（`refactor.yml` `a11y.yml` `colors.yml` `prefscore.yml` `notes.yml`）は完了後に削除済み

---

## 大規模変更のやり方（preview方式）

手作業ではなく、スクリプト＋GitHub Actions で機械的に処理する。

1. `scripts/` にPythonスクリプトを追加
2. `.github/workflows/` にワークフローを追加（`workflow_dispatch`、選択肢は `preview` / `apply`）
3. **preview で実行** → 出力先は `preview/` フォルダ（本番は無傷）
4. `https://ray-ray870.github.io/mitchie-fiscal/preview/` で確認
5. 問題なければ **apply**（`preview/` は自動削除）
6. 完了後、使ったワークフローを削除

**スクリプトの必須要件**
- 検証に1つでも失敗したら、ファイルを一切書き込まずに異常終了する
- preview モードでは本番ファイルに触れない
- 二重適用を防ぐガードを入れる（例：`if "calcHPref" in js: fail(...)`）
- `preview/` から親のデータを読むため、`data-*.json` 等のパスを `../` 付きに書き換える

---

## 開発上の原則

- **フォントサイズと可読性を最優先。** UI変更時に文字が小さくならないよう常に確認する
- 作業環境：Androidスマホのみ（Pixel 9a）。GitHubはブラウザ経由
- 新規ファイルは「Create new file で直接作る」または「Upload files」。既存ファイルの書き換えより、**追加 → 旧ファイル削除**の手順を好む
- GitHubでのファイル移動は「ファイル名の先頭にフォルダ名を足す」

---

## 未着手の課題

| 項目 | 内容 | 優先度 |
|---|---|---|
| インラインstyleのクラス化 | 192箇所のうち143箇所（55種類）が置換可能。残り49箇所は色を動的計算しているため対象外。上位5種類だけで79箇所 | 次にデザインを触るとき |
| 財政力指数の色分け | 市区町村の78%が橙になっている。4色化の案あり | 中 |
| 基金の色分け | 金額（30億/10億）で判定しており人口規模を無視。比率への変更案あり。ただし横浜市が橙になるなど直感と衝突する | 中 |
| 公会計の追加 | 総務省の市区町村指標一覧（1,741件）・都道府県指標一覧（47件）を取得済み。「みっちーのお家」として折りたたみセクションに、お家の古さ（有形固定資産減価償却率）・ローンの残り（将来世代負担比率）・借金の額・払い終わった割合の4指標を数字のみで表示する方針。イラストは作らない | 検討中 |
| iOSの共有機能 | `navigator.share` / `window.open` / `ClipboardItem` が非同期処理の後に呼ばれており、iOS Safari の「タップ直後」制約に抵触 | 声が届いてから |
| deploy-with-check | 構文チェック後に公開するワークフロー。ファイルは作成済みだが未導入。導入時は Settings → Pages の Source を「GitHub Actions」に変更する必要がある | 中 |
| iOS向けCSS | `-webkit-backdrop-filter` 未指定、`dvh` 未使用、`env(safe-area-inset-bottom)` 未指定 | 低 |
