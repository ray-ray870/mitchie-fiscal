# みっちー財政カルテ 仕様書

**記録日：2026年8月21日**
※ 本日のリファクタリング（ファイル分割・アクセシビリティ改善）を反映した実ファイルから作成

---

## 基本情報

| 項目 | 内容 |
|---|---|
| 公開URL | https://ray-ray870.github.io/mitchie-fiscal/ |
| リポジトリ | https://github.com/ray-ray870/mitchie-fiscal |
| ホスティング | GitHub Pages（mainブランチ） |
| GA4 測定ID | G-5BGX6KQZFL |
| キャッシュ版数 | `?v=202607251024` |

---

## ファイル構成

**2026年8月21日、単一ファイル構成から3ファイル構成へ分割済み。**

| ファイル | 行数 | 役割 |
|---|---|---|
| `index.html` | 129行 | 骨組み・メタタグ |
| `style.css` | 141行 | 見た目（**デザイン変更時はここだけ触る**） |
| `app.js` | 1,532行 | 診断ロジック・描画 |
| `images.js` | — | みっちー画像（Base64） |
| `manifest.json` | — | PWA設定 |
| `data-*.json` | 6ファイル | 財政データ |

### 外部読み込み（順序厳守）

1. GA4 タグ
2. `images.js`
3. html2canvas 1.4.1（CDN）
4. `app.js?v=...`

**`app.js` は必ず `images.js` と html2canvas より後**。順序が入れ替わると診断画像の生成が失敗する。

### 分割前の状態（参考）

分割前は `index.html` 1,792行・186KB の単一ファイル。中身は変更していないため、動作・見た目は分割前と完全に同一。

---

## データ構成

**全1,786エントリ**（全国の市区町村＋都道府県。東京23区は市区町村レベルの個別エンティティとして収録）

| ファイル | 件数 |
|---|---|
| `data-hokkaido-tohoku.json` | 413 |
| `data-chubu.json` | 325 |
| `data-kanto.json` | 323 |
| `data-kyushu.json` | 281 |
| `data-kinki.json` | 234 |
| `data-chugoku-shikoku.json` | 210 |

- 財政データ：**令和6年度**まで
- 人口データ：**令和7年**まで
- 出典：総務省「地方財政状況調査関係資料」
- 全フィールド実数値（推計値なし）

### JSONキー構造

| キー | 内容 | 履歴 |
|---|---|---|
| `p` | 都道府県名 | — |
| `f` | 財政力指数 | `_r1`〜`_r6` |
| `d` | 実質公債費比率 | `_r1`〜`_r6` |
| `x` | 経常収支比率 | `_r1`〜`_r6` |
| `u` | 将来負担比率 | `_r1`〜`_r6` |
| `r` | 財政調整基金残高（億円） | `_r1`〜`_r5` |
| `g` | 人口増減率（%） | `_r1`〜`_r6` |
| `pop` | 人口 | `_r1`〜`_r6` |
| `eo` / `ei` | 歳出 / 歳入（億円） | `_r1`〜`_r5` |
| `eog` / `eig` | 歳出増減率 / 歳入増減率（%） | — |
| `edu` | 教育費一般財源比率（%） | `_r1`〜`_r5` |
| `ch` | 子ども1人当たり投資額（万円） | `_r1`〜`_r5` |

最大8データポイントのローリング更新。

### 特殊処理

- **泊村**：北海道・青森県の名称衝突に対応
- **梼原町 / 須恵町**：異体字ゆらぎに対応
- 廃止済み自治体は除外

---

## スコア算出

**総合財政健全度スコア（100点満点）**

| 指標 | 配点 |
|---|---|
| 財政力指数 | 25点 |
| 実質公債費比率 | 20点 |
| 経常収支比率 | 20点 |
| 将来負担比率 | 20点 |
| 基金残高比率（財政調整基金残高÷歳出） | 15点 |

### 判定ラベル

| スコア | みっちーの状態 |
|---|---|
| 85点以上 | 絶好調 |
| 70点以上 | 元気 |
| 50点以上 | ちょっとしんどい |
| 30点以上 | ぐったり |
| 30点未満 | ひんし状態 |

---

## 主な機能

- 自治体名オートコンプリート検索
- 都道府県内ランキング・全国ランキング
- 同規模自治体との比較（A〜I の財政類型ラベル）
- データの急増・急落に対する動的な注釈表示
- 都道府県／市区町村それぞれの理由バッジ（9指標）
- 検索履歴（localStorage・5件）
- History API による端末の戻るボタン対応
- 診断結果の画像共有（html2canvas）
- OGP・`?city=` ディープリンク
- PWA（オフライン対応、Service Worker なし）

---

## アクセシビリティ

**2026年8月21日実施。見た目は一切変更していない。**

| 対応内容 | 件数 |
|---|---|
| `role="button"` + `tabindex="0"` | 11箇所（指標パネル s0〜s7、メーター m0/m1、比較ボタン） |
| 画面非表示の見出し（`.sr-only`） | 3箇所（診断結果／健康度スコア／財政指標の一覧） |
| みっちー画像の説明文（体調名を動的挿入） | 3箇所 |
| 検索候補の読み上げ（`aria-live`） | 1箇所 |
| キーボード操作（Enter / スペース） | 全 `role="button"` 対象 |

`style.css` 末尾に `.sr-only` とフォーカス表示のCSSを追加済み。

---

## 年次データ更新（GitHub Actions 半自動）

**次回リマインダー：10月20日**

### 手順

1. Claude に新年度のExcel URL（6種類）を探してもらう
2. `scripts/config.json` を編集
   - `fiscal_year_label` / `population_year_label` を更新
   - 各 `fiscal_indicators` のURLを差し替え
3. Actions から `update-data.yml` を実行（検証成功時のみ自動コミット）
4. `index.html` の年度表記を手動更新

### ⚠️ 毎回必ず確認

**OGPメタタグの自治体数「1,786」は手動更新が必要。**
`og:description` と `twitter:description` の2箇所。共有ボタンの文言はライブデータから自動生成されるが、**OGPのみハードコード**のため反映されない。

詳細手順は `scripts/README.md` を参照。

---

## 現在のワークフロー

| ファイル | 用途 |
|---|---|
| `.github/workflows/update-data.yml` | 年次データ更新 |

※ `refactor.yml` `a11y.yml` は作業完了後に削除済み

## scripts フォルダ

| ファイル | 用途 |
|---|---|
| `update_fiscal_data.py` | 年次データ更新（現役） |
| `config.json` | 更新対象URL・年度ラベル |
| `README.md` | 年次更新手順書 |
| `refactor_split.py` | ファイル分割（実行済み・記録用） |
| `refactor_a11y.py` | アクセシビリティ改善（実行済み・記録用） |

---

## 開発上の原則

- **フォントサイズと可読性を最優先**。UI変更時に文字が小さくならないよう常に確認する
- デプロイ手順：ファイルをダウンロード → GitHubモバイルで旧ファイルを削除 → 新ファイルをアップロード
- 大規模な一括変更は、手作業ではなく **`preview/` フォルダ方式**で行う
  1. スクリプトを `preview/` に出力（本番は無傷）
  2. `https://ray-ray870.github.io/mitchie-fiscal/preview/` で確認
  3. 問題なければ `apply` で本番へ昇格

---

## 流入状況

- X（Twitter）が全体の約80%
- LINE OpenChat 経由は WebView の制約で `(direct)` として計上される
- PWA のスタンドアロン起動も `(direct)` として計上される

---

## 未着手の課題

| 項目 | 内容 | 優先度 |
|---|---|---|
| インラインstyleのクラス化 | 192箇所のうち143箇所（55種類）が置換可能。残り49箇所は色を動的計算しているため対象外。上位5種類だけで79箇所 | 次にデザインを触るとき |
| iOSの共有機能 | `navigator.share` / `window.open` / `ClipboardItem` が非同期処理の後に呼ばれており、iOS Safari の「タップ直後」制約に抵触。無反応になる可能性 | 声が届いてから |
| スコア説明の表現 | 「AIが独自に算出」→「独自配点による参考スコア」等への変更を検討中（保留） | 保留 |
| iOS向けCSS | `-webkit-backdrop-filter` 未指定（iOS 17以前でぼかしが効かない）、`dvh` 未使用、`env(safe-area-inset-bottom)` 未指定 | 低 |
