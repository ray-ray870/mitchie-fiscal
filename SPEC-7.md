# みっちー財政カルテ 仕様書

**最終更新：2026年9月9日（共有機能の変更を反映）**
※ 公開中の実ファイルを読み取って作成

---

## ⚠️ 最重要：公開には手動の一手が必要

Pages の公開元を GitHub Actions に切り替えたため、**ファイルを更新しただけでは公開されません。**

**GitHubの仕様上、ワークフロー（bot）がコミットした変更は、別のワークフローを起動しません。** 無限ループを防ぐための安全装置です。

| 操作 | 自動で公開されるか |
|---|---|
| Rayさんが手でファイルを編集・アップロード | ✅ 自動で公開される |
| `preview` / `apply` を実行 | ❌ **手動実行が必要** |
| `update-data.yml`（年次更新）を実行 | ❌ **手動実行が必要** |
| `fetch_sfs.py`（標準財政規模の取込）を実行 | ❌ **手動実行が必要** |

**手動実行の手順**
1. **行動（Actions）** → **「構文チェック後にデプロイ」**
2. **ワークフローを実行する** → 実行
3. 1〜2分で公開

preview で確認するときも、apply で本番に出すときも、**そのたびに押す**必要があります。

---

## 基本情報

| 項目 | 内容 |
|---|---|
| 公開URL | https://ray-ray870.github.io/mitchie-fiscal/ |
| リポジトリ | https://github.com/ray-ray870/mitchie-fiscal |
| ホスティング | GitHub Pages（**Source は「GitHub Actions」**） |
| GA4 測定ID | G-5BGX6KQZFL |
| キャッシュ版数 | `style.css` / `app.js` は `?v=202607251024`、`images.js` は `?v=20260910`（**個別に管理。全ファイル同一だと思い込まないこと**） |

---

## ファイル構成

| ファイル | 行数 | 役割 |
|---|---|---|
| `index.html` | 129行 | 骨組み・メタタグ |
| `style.css` | 158行 | 見た目（**デザイン変更はここだけ**） |
| `app.js` | 1,724行 | 診断ロジック・描画 |
| `images.js` | — | みっちー画像（Base64） |
| `manifest.json` | — | PWA設定 |
| `data-*.json` | 6ファイル | 財政データ |
| `SPEC.md` | — | 本書 |

### 外部読み込み（順序厳守）

1. GA4 タグ
2. `images.js`
3. html2canvas 1.4.1（CDN）
4. `app.js?v=...`

**`app.js` は必ず `images.js` と html2canvas より後。** 順序が入れ替わると診断画像の生成が失敗する。deploy-with-check がこの順序を検査している。

**⚠️ 2026年9月9日に判明した重要な注意点**：`app.js` の冒頭に、`topImg`（トップ画面のみっちー画像）へ画像データを直接代入する行がある。もし今後**画像データをハードコードで直書きする改修**をした場合、`images.js` 側をいくら更新してもそちらが優先されて反映されない不具合が起きる。**画像を差し替える処理は必ず `images.js` の `IMGS.xxx` を参照する形にすること。** また `images.js` の `<script>` タグには他の読み込みタグ同様に `?v=` のバージョン指定を付けること（付いていないとブラウザキャッシュで更新が反映されない）。

---

## データ構成

**全1,786エントリ**（全国の市区町村＋都道府県。東京23区も個別収録）

| ファイル | 件数 |
|---|---|
| `data-hokkaido-tohoku.json` | 413 |
| `data-chubu.json` | 325 |
| `data-kanto.json` | 323 |
| `data-kyushu.json` | 281 |
| `data-kinki.json` | 234 |
| `data-chugoku-shikoku.json` | 210 |

- 財政データ：**令和6年度**まで
- 人口データ：**令和7年**まで
- 出典：総務省「地方財政状況調査関係資料」「決算カード」

### JSONキー構造

| キー | 内容 |
|---|---|
| `p` | 都道府県名（**都道府県は `p` が自分自身の名前と一致する**） |
| `f` | 財政力指数 |
| `d` | 実質公債費比率（%） |
| `x` | 経常収支比率（%） |
| `u` | 将来負担比率（%） |
| `r` | 財政調整基金残高（億円） |
| **`sfs`** | **標準財政規模（億円）** ← 2026年9月8日追加 |
| `g` | 人口増減率（%） |
| `pop` | 人口 |
| `eo` / `ei` | 歳出 / 歳入（億円） |
| `eog` / `eig` | 歳出増減率 / 歳入増減率（%） |
| `edu` | 教育費一般財源比率（%） |
| `ch` | 子ども1人当たり投資額（万円） |

末尾に `_r1`〜`_r6` を付けたキーが過去年度の値。最大8データポイントのローリング更新。
**`sfs` に履歴はない**（令和6年度の値のみ）。

**都道府県の判定方法：** 読み込み時に `DB[k].p === k` を調べ、一致するものに `__pref = true` を立てている。

### 特殊処理

- **泊村**：北海道・青森県の名称衝突に対応
- **梼原町 / 須恵町**：異体字ゆらぎに対応
- **同名自治体54件**：「金山町（山形）」のように括弧付きのキーで区別
- 廃止済み自治体は除外

### 既知の欠落

**宮崎県 日之影町** と **高知県 大月町** が収録されていない。総務省の公会計データには存在するため、次回の年次更新時に確認すること。

---

## 標準財政規模（sfs）について

2026年9月8日に追加。**財政調整基金の判定を、世間で使われる物差しに合わせるため。**

### なぜ必要だったか

財政調整基金の水準は、実務では「標準財政規模に対する割合」で語られる。ところが本アプリだけが歳出を分母にしていたため、世間の数字と比較できなかった。

他の指標（実質公債費比率・将来負担比率・実質赤字比率）は、もともと総務省が標準財政規模を分母にして算定している。**sfs を入れたことでアプリ内の分母が揃った。**

副次的に、**災害で歳出が一時的に膨らんだ自治体が不当に低評価される歪みも解消**された（珠洲市36→48点、輪島市42→52点）。

### 取得方法

総務省の**決算カード**から取得する。`scripts/fetch_sfs.py` が自動で行う。

| 対象 | ファイル | セル位置 |
|---|---|---|
| 市区町村 | 都道府県ごとに**48ファイル**（北海道のみ2分割） | **54行97列**（ラベルは84列） |
| 都道府県 | 1ファイル | **37行95列**（ラベルは81列） |

1シート＝1自治体。シート名は「2鹿児島市」のように先頭に数字が付くので、除去して突合する。**特別区23区も東京都のファイルに含まれる。**

### URLの規則

決算カードのURLは毎年変わる通し番号。**PDFとExcelが交互に並ぶため、1団体あたり2ずつ増える。**

```
番号 = base_muni + (並び順 - 13) × 2
※ 北海道1（並び順0）のみ +1 の例外
```

`config.json` の `sfs.base_muni` に**東京都のExcelの番号**を書けば、残り47件は計算で求まる。令和6年度は `1063971`。

**毎年ここだけ確認して書き換える。** 番号がずれた年は404が出るので実行時に検出できる。

### 名前の正規化

決算カードのシート名は「六ケ所村」と大きい「ケ」を使う一方、アプリ側は「六ヶ所村」と小書き。`norm()` で吸収している。これがないと7件が突合できない。

### 実行方法

常設の `.github/workflows/fetch-sfs.yml` から実行する（2026年9月に常設化済み。以前は毎年一時的に作り直していた）。

```yaml
- run: pip install openpyxl
- run: python scripts/fetch_sfs.py
```

48ファイルを順に取得するため**9〜15分かかる**。突合率が95%未満なら書き込まずに停止する。**令和6年度は1,786/1,786件（100%）で突合成功。**

---

## スコア算出

市区町村と都道府県で計算式が異なる。`calcH(f,d,x,u,r,eo,isPref,sfs)` の `isPref` が真なら `calcHPref` に分岐する。

### 市区町村（`calcH` 本体）

| 指標 | 配点 | 計算 |
|---|---|---|
| 財政力指数 | 25点 | `f/1.2*25` |
| 実質公債費比率 | 20点 | 25%で0点 |
| 経常収支比率 | 20点 | 100%で0点、85%で満点 |
| 将来負担比率 | 20点 | 200%で0点、0以下で満点 |
| **基金比率** | 15点 | **標準財政規模の20%で満点** |

結果：1,739件の中央値**68点**、28〜100点に分布。

### 都道府県（`calcHPref`）

市区町村基準では47県中46県が下位2段階に固まっていたため新設。都道府県は高校・国道・河川など大規模資産を抱え、将来負担比率や経常収支比率の水準が構造的に高い。

| 指標 | 配点 | 0点ライン | 満点ライン | 47県の中央値 |
|---|---|---|---|---|
| 財政力指数 | 25点 | 0.20 | 0.90 | 0.47 |
| 実質公債費比率 | 20点 | 20% | 6% | 11.0% |
| 経常収支比率 | 20点 | 101% | 86% | 93.8% |
| 将来負担比率 | 20点 | 340% | 80% | 159.7% |
| **基金比率** | 15点 | — | **標準財政規模の10%** | 6.4% |

結果：47県の中央値**55点**、17〜100点。

**市区町村とは約13点の差が残るが、役割が違う組織なので統一しない。** アプリ内の解説に明記済み。

### 判定ラベル

| スコア | みっちーの状態 |
|---|---|
| 85点以上 | 絶好調 |
| 70点以上 | 元気 |
| 50点以上 | ちょっとしんどい |
| 30点以上 | ぐったり |
| 30点未満 | ひんし状態 |

---

## 指標の色分け

### 財政力指数（`colorF`）

| 範囲 | 色 |
|---|---|
| 0.70以上 | 🟢 緑 |
| 0.45〜0.70 | 🔵 青 |
| 0.25〜0.45 | 🟡 黄 |
| 0.25未満 | 🟠 橙 |

市区町村 緑22%・青28%・黄30%・橙21%／都道府県 緑13%・青45%・黄43%・橙0%。

**根拠：** 中央値は市区町村0.44・都道府県0.47。旧基準（1.0/0.7）では市区町村の78%が橙になり判別できなかった。財政力が低いこと自体は地方交付税で補われる前提の数字であり危険信号ではないため、橙は下位2割程度に絞っている。

### 経常収支比率（`colorX`／市区町村・都道府県 共通）

| 範囲 | 色 |
|---|---|
| 90%未満 | 🟢 緑 |
| 90〜95% | 🔵 青 |
| 95〜98% | 🟡 黄 |
| 98%以上 | 🟠 橙 |

**根拠：** かつて「75〜80%が望ましい」とされたが法令基準ではなく慣例的な目安。社会保障費の増加で全国的に上昇し、2003年度以降は全国平均が90%超。中央値は市区町村91.5%・都道府県93.8%。98%以上は全体の約9%。

### 将来負担比率（`colorU`／基準を分ける）

**市区町村**：負担なし・0以下→緑／60%未満→青／60〜100%→黄／100%以上→橙
**都道府県**：100%未満→緑／100〜160%→青／160〜250%→黄／250%以上→橙

※ 350%以上は法令上の早期健全化基準（都道府県は400%）。

### 財政調整基金（`colorR` / `reserveBands`）

**分母は標準財政規模。** 表示・色・判定文・グラフ・スコア・スコア内訳のすべてが同じ基準を使う。

**市区町村**（中央値24.8%）

| 範囲 | 色 |
|---|---|
| 20%以上 | 🟢 一般に適正とされる上限に到達 |
| 10〜20% | 🔵 一般に適正とされる範囲 |
| 5〜10% | 🟡 やや少なめ |
| 5%未満 | 🟠 少ないほう |

**都道府県**（中央値6.4%）

| 範囲 | 色 |
|---|---|
| 10%以上 | 🟢 都道府県としては多いほう |
| 5〜10% | 🔵 総務省調査で最も多い水準 |
| 2.5〜5% | 🟡 やや少なめ |
| 2.5%未満 | 🟠 少ないほう |

**根拠：** 法令上の基準はない。総務省が平成29年に行った「地方公共団体の基金の積立状況等に関する調査」で、積立の考え方を「標準財政規模の一定割合」と答えた団体の水準は、**都道府県で5%以下・5〜10%、市町村で5〜10%・10〜20%が多かった**。この実務水準に合わせている。

`reserveIsAmple()` は他の指標の総合コメントでも使う共通判定で、**緑ライン（市区町村20%／都道府県10%）と一致**する。カードの色と文章が食い違わない。

### 変更していないもの

**実質公債費比率**（10/18/25）… 18%が起債許可、25%が早期健全化基準という**法令上の数値**なので動かさない。

---

## スコアと指標の食い違いを知らせる帯（`noteHtml`）

**スコアバーの直上**に1行表示する。

| 条件 | 文言 |
|---|---|
| 70点以上 かつ 指標が黄または橙 | ⚠️ ただし、〇〇は高い水準です |
| 50点未満 かつ 指標が緑 | 💡 〇〇は健全な水準です |

対象は経常収支比率・将来負担比率・実質公債費比率の3つ。

---

## 主な機能

- 自治体名オートコンプリート検索
- 都道府県内ランキング・全国ランキング
- 同規模自治体との比較（A〜I の財政類型ラベル）
- データの急増・急落に対する動的な注釈表示
- 財政状況バッジ（起債許可団体・早期健全化団体・財政再生団体）
- 検索履歴（localStorage・5件）
- History API による端末の戻るボタン対応
- 診断結果の画像共有（html2canvas）
- OGP・`?city=` ディープリンク
- PWA（Service Worker なし）

---

## Myみっちー機能（実装済み・本番未公開／今月中にapply予定）

自分の住む自治体を登録しておくと、次回訪問時にすぐ結果を見られる機能。**コードは実装・preview確認済みだが、意図的に本番へは出していない。** ⚠️ 当初は総務省データの年次更新（翌年3月末）と同時にapplyする方針だったが、公会計機能とあわせて**データ更新とは切り離して先行してapplyする**方針に変更した（2026年9月）。

### 画面構成

検索バーの下（少し余白を空けて）、紫のボタンが横に2つ並ぶ：「Myみっちー ▼」（左）と「全国のみっちー ▼」（右）。どちらもボタン単体の見た目・サイズは統一（幅は内容に合わせて自動、横幅を無理に揃えない）。片方を押すと、押した方のボタンが消えてカードに切り替わり、**もう片方のボタンも一緒に隠れる**（同時に2枚のカードが開くことはない）。閉じるのはカード右上の✕のみ。**毎回、ページ読み込み時は両方とも閉じた状態から始まる**（開閉状態は保存しない）。

### 登録の流れ

- 市区町村欄・都道府県欄の2つを表示（市区町村欄が先）
- 市区町村を確定すると、都道府県欄が自動で埋まる。都道府県欄は独立して変更可能
- 各欄の候補リストは、市区町村欄なら市区町村のみ、都道府県欄なら47都道府県のみに絞られる
- 確定方法は3通り：候補をタップ／共通の「登録」ボタン（1つ、両欄の下に横幅いっぱいで1つだけ。各欄ごとの個別ボタンは持たない）／Enterキー
- 登録モード中、入力欄の枠が紫に光る演出（`prefers-reduced-motion` 設定時は光らせず枠色のみ変更）。1文字でも入力すると光は止まる

### 保存

`localStorage`（キー: `mmReg`）に `{city, pref}` の形で保存。サーバーには送らない。市区町村と都道府県は独立した値として保存されるため、通常は連動するが、都道府県欄だけを後から変更した場合は市区町村とは別の都道府県が保存される（意図的な仕様）。

### カードの中身（登録済み時）

- 市区町村：スコア・状態ラベル・全国順位（全市区町村中）・県内順位を、それぞれ順位バー付きで表示
- 都道府県：スコア・状態ラベル・全国順位（47都道府県中）を表示
- 両方登録されていれば点線区切りで両方表示。片方のみならその項目だけ
- 順位バー：右へ行くほど上位（1位が右端）、色は左から橙→黄→青→緑のグラデーション、`IMGS.top` の画像30pxをバー上の該当位置（3〜97%にクランプ）に重ねる
- ボタン：登録されている項目ごとに「◯◯を見る」を個別表示（例：市区町村と都道府県の両方を登録していれば「姶良市を見る」「鹿児島県を見る」の2つ）＋「変更」ボタン

### 未登録時のカード

案内文＋「Myみっちーを登録する」ボタン。押すと登録フォームに切り替わる。

### 順位の計算ロジック

- 市区町村の全国順位：都道府県自身の行（`DB[k].p===k`）を除外した全市区町村でスコア降順に並べ、対象の位置を探す
- 市区町村の県内順位：同じ都道府県の市区町村のみに絞って同様に計算
- 都道府県の全国順位：都道府県の行（47件）のみでスコア降順に並べる
- スコアは既存の `calcH()` をそのまま使用（市区町村・都道府県で式が違うため混ぜない）

### 実データでの動作確認値（令和6年度）

- 姶良市 62点／全国1,216位・1,739自治体／鹿児島県内33位・43市町村
- 鹿児島県 39点／全国44位・47都道府県

---

## 個別の自治体ページのスコア枠（Myみっちーと同時にapply）

診断結果ページの「財政健全度スコア」枠（黄色い枠）の中身を、以下のように変更する。

- 変更前：スコアバー（0〜100の進捗バー）＋「タップでスコアの似た自治体と詳細を見る▶」
- 変更後：スコア数値のみを右上に表示＋Myみっちーと同じ順位バー（市区町村なら全国順位＋県内順位の2本、都道府県なら全国順位の1本）＋「タップで**〇〇市**と似た自治体と詳細を見る▶」（自治体名部分は目立つ色で強調）
- 枠をタップすると開く「似た自治体」の比較シート機能自体は変更なし

## 比較機能との連携（Myみっちーと同時にapply）

- 比較の上限を5→**6**に変更（Myみっちー登録分の枠を確保するため）
- 診断結果ページ右上、既存の「🔖 比較する」バッジの**下**（縦並び）に「Myみっちーと比較」ボタンを追加
- 押すと、今見ている自治体＋Myみっちー登録済みの自治体（市区町村・都道府県、登録があるだけ両方）が**まとめて**比較リストへ追加され、そのまま比較シートが開く
- 既にリストに入っているものは押しても増えない。全員追加済みなら「✓ 比較中」表示になり、その状態でもう一度押すと**トグルで全員リストから外れる**（既存の「比較する」ボタンと同じ挙動）
- 上限に達していて追加できない場合は「比較は6個までです。比較リストからあと◯個消してください。」と案内
- 診断結果ページの自治体名（例：姶良市）の**左側**に、Myみっちー登録先であれば🐧マークが付く

## 全国のみっちー機能（Myみっちーと同時にapply）

トップ画面の「全国のみっちー ▼」ボタンから開く、日本全体の財政状況を見るカード。2ブロック構成。

### ①全国の分布

絶好調〜ひんしの5段階それぞれの件数・割合（全1,786自治体、市区町村・都道府県を合算）を横棒グラフで表示。Myみっちー登録があれば、その下に「🐧 Myみっちー」に続けて、登録した市区町村・都道府県それぞれが「どのゾーンにいるか」を1行ずつ表示する（例：「鹿児島県 → 「ぐったり」」「姶良市 → 「ちょっとしんどい」」）。

### ②47都道府県ランキング

スコア降順で1位・2位・47位（最下位）のみを表示し、「47都道府県ぜんぶ見る▶」で全件展開（再度押すとたたむ）。Myみっちーで都道府県を登録していれば、その行に🐧マークと「（Myみっちー）」を付けてハイライト。

### 実装ファイル（apply前は未使用・削除しないこと）

- `scripts/refactor_mymitchie.py`：HTML/CSS/JSを注入するスクリプト（preview/apply両対応、二重適用ガードあり）。Myみっちー本体・スコア枠変更・比較連携・全国のみっちー・すべてこの1本のスクリプトでまとめて処理する
- `scripts/mymitchie_html.txt` / `mymitchie_css.txt` / `mymitchie_js.txt`：差し込む内容そのもの
- `.github/workflows/mymitchie.yml`

apply後は、上記5ファイルは削除してよい（コードはHTML/CSS/JSに直接書き込まれる）。

---

## 共有機能の変更（2026年9月・本番反映済み）

Myみっちー本体とは切り離し、以下2点は**このタイミングで直接本番へ反映済み**（10月まで待たない）。

- **Xで共有時の案内文**：画像をクリップボードにコピーした後のトースト表示を「画像がコピーされました！」（小さめ）＋「※本文に画像を貼り付けてください。」（大きく・黄色で強調）に変更
- **共有画像（結果カード）に順位を追加**：スコアの下に、全国順位／県内（都道府県の場合は全国のみ）順位を薄紫の枠で表示。右側の6指標バーは`justify-content:space-between`で左側の高さいっぱいに均等配置し、最後の「人口増減率」が順位枠の下端と揃うようにしている

この2点は `app.js`（`shareRankBoxHtml`・`shcRankNationalCity`等の専用関数を新設。Myみっちー側の`mm`系関数とは独立）と `style.css`（`.sc-rank-box`等）に直接実装済み。Myみっちー機能とは実装上完全に独立しているため、10月のapply作業では触れなくてよい。

---

## アクセシビリティ

| 対応内容 | 件数 |
|---|---|
| `role="button"` + `tabindex="0"` | 11箇所 |
| 画面非表示の見出し（`.sr-only`） | 3箇所 |
| みっちー画像の説明文（体調名を動的挿入） | 3箇所 |
| 検索候補の読み上げ（`aria-live`） | 1箇所 |
| キーボード操作（Enter / スペース） | 全 `role="button"` 対象 |

---

## 年次データ更新

**⚠️ 2026年9月11日に判明：10月更新は前提が間違っていた**

総務省の公表スケジュール表を確認したところ、アプリが使っている全データソースのうち最も早いものでも公表は12月、歳出内訳・決算カード・決算状況調は翌年2〜3月だった。つまり10月時点では**まだ何も新年度データが存在しない**。

**次回リマインダー：翌年3月末**（すべてのデータソースが出揃うタイミングに合わせて年1回まとめて取り込む方針に変更）

総務省の公表時期の目安（令和6年度決算の実績から算出。年度が1つ進むごとに同じ月にずれる）：

| データ | 使用箇所 | 公表時期の目安 |
|---|---|---|
| 主要財政指標一覧 | `fiscal_indicators`（財政力指数・経常収支比率など） | 決算年度の翌々年12月頃 |
| 普通会計決算の概要 | 参考情報 | 決算年度の翌々年11月頃 |
| 都道府県決算状況調 | `budget`/`purpose_expenditure` | 決算年度の翌々年2月頃 |
| 市町村別決算状況調 | `budget`/`purpose_expenditure` | 決算年度の翌々年3月頃 |
| 決算カード・財政状況資料集 | `sfs`（標準財政規模） | 決算年度の翌々年3月頃 |
| 統一的な基準による財務書類（公会計9指標） | `kokaikei.json` | 不定期・1〜2年遅れ（令和5年度分が最新） |

**3月末に、まとめて実施する手順（更新版）**

1. Claude に新年度のExcel URL（6種類）を探してもらう
2. `scripts/config.json` を編集（URL、`fiscal_year_label`、`population_year_label`）
3. Actions から **`update-data.yml`** を実行
4. **行動 →「構文チェック後にデプロイ」→ 実行** ← 忘れやすい
5. **決算カードの `sfs.base_muni` を新年度の番号に書き換え、`fetch_sfs.py` を実行**（常設の「標準財政規模の取得（sfs）」ワークフローから実行）
6. 再び **「構文チェック後にデプロイ」→ 実行**
7. `index.html` の年度表記を手動更新
8. **公会計データ（`kokaikei.json`）も、新しいExcelが総務省サイトに出ていれば`build_kokaikei.py`で更新**（不定期公表のため、出ていない年はスキップ）

### ⚠️ 毎回必ず確認

**OGPメタタグの自治体数「1,786」は手動更新が必要。**
`og:description` と `twitter:description` の2箇所。共有ボタンの文言はライブデータから自動生成されるが、**OGPのみハードコード**のため反映されない。

詳細手順は `scripts/README.md` を参照。

---

## ワークフローとスクリプト

### 常設のワークフロー

| ファイル | 用途 |
|---|---|
| `deploy-with-check.yml` | 公開前チェック＋デプロイ（`push` と手動の両方で起動） |
| `update-data.yml` | 年次データ更新 |
| `fetch-sfs.yml` | 標準財政規模（sfs）の取得。`scripts/fetch_sfs.py` を実行 |

一度きりの作業用ワークフローは、完了後に削除している。

### scripts フォルダ

| ファイル | 用途 |
|---|---|
| `update_fiscal_data.py` | 年次データ更新（現役） |
| **`fetch_sfs.py`** | **標準財政規模の取得（毎年使う）** |
| `config.json` | 更新対象URL・年度ラベル・`sfs.base_muni` |
| `README.md` | 年次更新手順書 |
| `refactor_*.py` / `reserve_block.txt` | 実行済みの一時スクリプト（記録用） |

### deploy-with-check の検査項目（7つ）

1. `app.js` の構文
2. `images.js` の構文
3. 必要ファイル11個の存在
4. `style.css` / `app.js` の読み込みタグ
5. 読み込み順序とGA4・OGPタグの残存
6. `<div>` の開閉バランス
7. データ6ファイルの妥当性と**合計1,700件以上**

チェックに落ちた場合は公開されず、前回公開されたサイトがそのまま残る。

---

## 大規模変更のやり方（preview方式）

1. `scripts/` にPythonスクリプトを追加
2. `.github/workflows/` にワークフローを追加（`workflow_dispatch`、選択肢は `preview` / `apply`）
3. **preview で実行** → 出力先は `preview/` フォルダ（本番は無傷）
4. **「構文チェック後にデプロイ」を手動実行**
5. `https://ray-ray870.github.io/mitchie-fiscal/preview/` で確認
6. 問題なければ **apply**（`preview/` は自動削除）
7. **再び「構文チェック後にデプロイ」を手動実行**
8. 完了後、使ったワークフローを削除

**スクリプトの必須要件**
- 検証に1つでも失敗したら、ファイルを一切書き込まずに異常終了する
- preview モードでは本番ファイルに触れない
- 二重適用を防ぐガードを入れる（例：`if "colorF" in js: fail(...)`）
- `preview/` から親のデータを読むため、`data-*.json` 等のパスを `../` 付きに書き換える

---

## 開発上の原則

- **フォントサイズと可読性を最優先。** UI変更時に文字が小さくならないよう常に確認する
- 作業環境：Androidスマホのみ。GitHubはブラウザ経由
- 新規ファイルは「Create new file で直接作る」または「Upload files」。既存ファイルの書き換えより、**追加 → 旧ファイル削除**の手順を好む
- GitHubでのファイル移動は「ファイル名の先頭にフォルダ名を足す」
- **削除するファイル名は実行前によく確認する**（deploy-with-check.yml を誤って消した事例あり）
- **基準を決めるときは、必ず実データの分布か公的な調査結果を根拠にする。** 感覚で数字を置かない

---

## 未着手の課題

| 項目 | 内容 | 優先度 |
|---|---|---|
| インラインstyleのクラス化 | 192箇所のうち143箇所（55種類）が置換可能。残り49箇所は色を動的計算しているため対象外 | 次にデザインを触るとき |
| 標準財政規模の履歴 | 現在は令和6年度の1点のみ。過去年度分を取り込めば推移グラフが出せるが、年度ごとに48ファイル必要 | 低 |
| 公会計の追加 | 総務省の市区町村指標一覧（1,741件）・都道府県指標一覧（47件）を取得済み。「みっちーのお家」として折りたたみセクションに、お家の古さ（有形固定資産減価償却率）・ローンの残り（将来世代負担比率）・借金の額・払い終わった割合の4指標を数字のみで表示する方針。イラストは作らない | 検討中 |
| iOSの共有機能 | `navigator.share` / `window.open` / `ClipboardItem` が非同期処理の後に呼ばれており、iOS Safari の「タップ直後」制約に抵触 | 声が届いてから |
| iOS向けCSS | `-webkit-backdrop-filter` 未指定、`dvh` 未使用、`env(safe-area-inset-bottom)` 未指定 | 低 |
| デプロイの完全自動化 | botのコミット後もデプロイが走るようにするには、各ワークフローからデプロイを呼び出す設定が必要。トークン設定を伴うためスマホ作業ではやや面倒 | 低 |
