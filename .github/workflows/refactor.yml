name: ファイル分割リファクタ

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "preview か apply を選ぶ"
        required: true
        default: "preview"
        type: choice
        options:
          - preview
          - apply

jobs:
  refactor:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python scripts/refactor_split.py ${{ inputs.mode }}
      - if: ${{ inputs.mode == 'apply' }}
        run: rm -rf preview
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --staged --quiet; then
            echo "変更なし"
          else
            git commit -m "リファクタ: ファイル分割 (${{ inputs.mode }})"
            git push
          fi
